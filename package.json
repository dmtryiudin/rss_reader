{
  "devDependencies": {
    "turbo": "^1.13.0"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm i && turbo run dev",
    "build": "turbo run build",
    "start": "node apps/api/dist/main"
  }
}
